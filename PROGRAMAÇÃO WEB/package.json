{
  "name": "app_agendamento_consulta",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setadmin": "node -e \"const db=require('./src/config/database'); const U=require('./src/models/UsuarioModel'); db.sync().then(()=>U.update({isAdmin:true},{where:{email:process.argv[1]}}).then(()=>console.log('Permissao concedida com sucesso ao email: ' + process.argv[1])))\""
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^4.21.2",
    "express-session": "^1.17.2",
    "mustache-express": "^1.3.2",
    "sequelize": "^6.31.1",
    "sqlite3": "^5.1.6"
  }
}
